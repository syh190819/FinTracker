/** App header — fixed top bar with title only */
const Header: React.FC = () => (
  <div className="fixed-header">
    <div className="header-inner">
      <h1>FinTracker</h1>
    </div>
  </div>
);

export default Header;
