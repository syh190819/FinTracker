# Dependencies
node_modules/

# Build / cache artifacts
backend/target/
frontend/dist/
.vite/

# Local environment config (contains secrets)
backend/.env

# Runtime logs
*.log*

# Database backups (contain sensitive data)
backend/backup_*.sql

# Editor / tool memory
.workbuddy/

# Stray root-level lockfile from accidental npm init
/package-lock.json
